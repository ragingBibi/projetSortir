{% extends 'base.html.twig' %}

{% form_theme update_event_form 'bootstrap_5_layout.html.twig' %}

{% block title %}Modifier un évènement{% endblock %}

{% block pageName %}Modifier un évenement{% endblock %}

{% block body %}

    <div class="flex justify-content">
        {{ form_start(update_event_form) }}
            {{ form_widget(update_event_form) }}
        {{ form_end(update_event_form) }}
    </div>

{#    TODO : ajouter les actions suivantes#}
{#    <div>#}
{#        <button type="submit">Publier</button>#}
{#        <button><a href="{{ path('event_delete', {'id': event.id}) }}" onclick="return confirm('Etes-vous sûr de vouloir supprimer cet évènement ?') }}"></a>Supprimer</button>#}
{#    </div>#}

    <a href="{{ path('home_home') }}">Retour à l'accueil</a>

{% endblock %}
