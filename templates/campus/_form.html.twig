{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary mx-4 my-3">{{ button_label|default('Enregistrer') }}</button>
    <a href="{{ path('app_campus_index') }}" class="link-secondary"> Retour à la liste </a>
{{ form_end(form) }}
