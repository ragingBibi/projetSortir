<h1>Paix sur toi jeune terrien \\//</h1>

<p> Pour réinitialiser ton mot de passe, cliquer sur le lien ci-dessous tu devras : </p>

<a href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password', {token: resetToken.token}) }}</a>

<p> Ce lien s'autodétruira dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>

<p> Bazinga!</p>
